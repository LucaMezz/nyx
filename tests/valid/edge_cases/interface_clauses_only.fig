// Edge case: interface with only clauses, no methods
interface Allocator[T]
    extends
        Display
        Debug
    where
        T: Mem

interface MustBeOrdered[T]
    where
        T: Ord

interface ExtendsOnly
    extends
        Display
        Debug
